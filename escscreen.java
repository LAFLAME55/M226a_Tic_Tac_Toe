import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Das ist der Endscreen. Falls man die ESC Taste gedrückt hat, so wird das Spiel beendet. Diese Welt besteht nur aus einem Bild. 
 * 
 * @Serkan Tugra & Julian Schmid
 * @Version : 1.0.0 
 */
public class escscreen extends World
{
    /**
     * Constructor for objects of class escscreen.
     * 
     */
    // Erstellt eine Welt à 660 x 400 px
    public escscreen()
    {    
        // Create a new world with 600x400 cells with a cell size of 1x1 pixels.
        super(600, 400, 1); 
    }
}
